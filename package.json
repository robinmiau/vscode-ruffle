{
  "name": "ruffle",
  "displayName": "Ruffle",
  "description": "Open and play SWF files in VS Code using Ruffle",
  "version": "0.2.0",
  "publisher": "RobinMiau",
  "author": {
    "name": "RobinMiau",
    "url": "https://github.com/robinmiau"
  },
  "license": "MIT",
  "icon": "resources/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/robinmiau/vscode-ruffle.git"
  },
  "bugs": {
    "url": "https://github.com/robinmiau/vscode-ruffle/issues"
  },
  "engines": {
    "vscode": "^1.103.0"
  },
  "categories": [
    "Visualization",
    "Other"
  ],
  "keywords": [
    "flash",
    "swf",
    "ruffle",
    "actionscript",
    "player",
    "viewer"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "customEditors": [
      {
        "viewType": "ruffle.player",
        "displayName": "Ruffle Player",
        "selector": [
          {
            "filenamePattern": "*.swf"
          }
        ]
      }
    ],
    "configuration": {
      "title": "Ruffle",
      "properties": {
        "ruffle.autoplay": {
          "type": "string",
          "default": "on",
          "enum": [
            "auto",
            "off",
            "on"
          ],
          "description": "Controls autoplay behavior for SWF files"
        },
        "ruffle.letterbox": {
          "type": "string",
          "default": "fullscreen",
          "enum": [
            "fullscreen",
            "off",
            "on"
          ],
          "description": "Controls how areas outside the movie stage are handled"
        },
        "ruffle.contextMenu": {
          "type": "string",
          "default": "on",
          "enum": [
            "on",
            "rightClickOnly",
            "off"
          ],
          "description": "Controls when the context menu is shown"
        },
        "ruffle.scale": {
          "type": "string",
          "default": "showAll",
          "enum": [
            "showAll",
            "noborder",
            "exactfit",
            "noscale"
          ],
          "description": "Controls the movie's initial stage scale mode"
        },
        "ruffle.quality": {
          "type": "string",
          "default": "high",
          "enum": [
            "low",
            "medium",
            "high",
            "best"
          ],
          "description": "Controls the rendering quality (affects MSAA)"
        },
        "ruffle.keepActive": {
          "type": "boolean",
          "default": true,
          "description": "Keep the Ruffle player running when switching to another tab, preventing it from restarting. Takes effect after reloading the window."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "webpack",
    "watch": "webpack --watch",
    "package": "webpack --mode production --devtool hidden-source-map",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test",
    "version": "conventional-changelog -p angular && git add CHANGELOG.md"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "25.x",
    "@types/vscode": "^1.103.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "conventional-changelog": "^7.2.0",
    "conventional-changelog-angular": "^8.3.1",
    "copy-webpack-plugin": "^14.0.0",
    "eslint": "^10.1.0",
    "ts-loader": "^9.5.4",
    "typescript": "^6.0.0",
    "typescript-eslint": "^8.58.0",
    "webpack": "^5.105.4",
    "webpack-cli": "^7.0.2"
  },
  "dependencies": {
    "@ruffle-rs/ruffle": "^0.2.0-nightly.2026.4.1"
  }
}
